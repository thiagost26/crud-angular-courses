import { Routes } from '@angular/router';
import { GerarTabelaComponent } from './gerar-tabela.component';

export const TABELA_ROUTES: Routes = [
  { path: 'tabela', component: GerarTabelaComponent },
];
